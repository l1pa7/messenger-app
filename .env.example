# PostgreSQL
DB_USER=messenger
DB_PASSWORD=СЮДА_НАДЁЖНЫЙ_ПАРОЛЬ
DB_NAME=messenger

# Redis
REDIS_PASSWORD=СЮДА_REDIS_ПАРОЛЬ

# MinIO (хранилище файлов)
MINIO_USER=minioadmin
MINIO_PASSWORD=СЮДА_MINIO_ПАРОЛЬ
MINIO_BUCKET=messenger-files

# JWT — сгенерируй командой: openssl rand -hex 32
JWT_SECRET=СЮДА_ДЛИННЫЙ_СЛУЧАЙНЫЙ_КЛЮЧ

# Cloudflare Tunnel (заполни после создания туннеля)
CF_TUNNEL_TOKEN=
